{
  "title": "ambrosus.event.location.asset",
  "description": "Location expressed with an assetId",
  "type": "object",
  "properties": {
    "type": {
      "description": "Event entry type",
      "type": "string",
      "const": "ambrosus.event.location.asset"
    },
    "asset": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{64}$"
    }
  },
  "required": ["type", "asset"]
}
